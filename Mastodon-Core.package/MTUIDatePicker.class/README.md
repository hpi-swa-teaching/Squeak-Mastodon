A MTUIDatePicker is a UI component for picking a date and time.
It has text fields for the year, month, day, hour and minute of the date and time.
You can switch between fields using tab and shift-tab.
The input is only accepted if it yields a grammatically correct date, but the date is not tested for actual validity.